<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/no_users_yet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center_horizontal"
            android:text="@string/no_driver_on_jc"
            android:textAlignment="center"
            android:textColor="@color/hint_color"
            android:textStyle="bold"
            android:visibility="@{driversCount > 0? View.GONE: View.VISIBLE}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/accounts_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="2dp"
            android:visibility="@{driversCount > 0? View.VISIBLE: View.GONE}"
            tools:listitem="@layout/driver_view" />

    </FrameLayout>

    <data>
        <import type="android.view.View"/>
        <variable
            name="driversCount"
            type="Integer" />
    </data>

</layout>
